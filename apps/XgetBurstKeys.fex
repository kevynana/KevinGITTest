-*XgetBurstKeys.fex
-*switch the keys, and Save IKEY to NIKEY
-SET &&NIKEY = &&IKEY;
JOIN CLEAR *
JOIN BREAK_KEY IN XMAPKEYS TO BREAK_KEY IN BURST_GROUPS AS BB
END

TABLE FILE XMAPKEYS
PRINT MEMBER
BY INST_KEY 
BY BREAK_LEVEL
WHERE NINSTKEY EQ &&IKEY
ON TABLE SAVE AS SWKEYS
END
-RUN

-READ SWKEYS &&IKEY.7. &&LEVEL.2.  &&MEMBER.15.
-TYPE &&IKEY &&LEVEL &&MEMBER
-SET &&LOOPCOUNTER  = &&NIKEY;