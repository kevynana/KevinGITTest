
JOIN SET_KEY IN RPT_INST TO SET_KEY IN RPT_SET
END
TABLE FILE RPT_INST
PRINT GLOBAL_PARM 
BY ROLLUP_CODE
BY USER_NAME
BY SET_NAME
WHERE USE_PARM EQ 'AVIW59'
WHERE SET_NAME OMITS 'Special' OR 'Default' 
WHERE SET_NAME OMITS 'Test' OR 'TEST' OR 'Demo' OR 'DEMO'
END

