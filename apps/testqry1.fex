SET ASNAMES=ON

JOIN SET_KEY IN RPT_INST TO SET_KEY IN RPT_SET
END

TABLE FILE RPT_INST
COUNT RPT_NAME
BY USER_NAME
BY SET_NAME
WHERE ROLLUP_CODE EQ 'CH2M-R'
WHERE USER_NAME NE ' ' OR 'master' OR 'steve' OR 'joy' OR 'deb' 
                OR 'wtorres' OR 'tarnold' OR 'gracec' OR 'margiec'
ON USER_NAME PAGE-BREAK
END





