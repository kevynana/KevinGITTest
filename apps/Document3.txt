

-SET &WHATDATE = IF '&&DATETYPE.EVAL' EQ 'I' THEN 'TRN_DATE' ELSE 'FST_DPT_DATE';
-SET &AIRDATES = &&WEB_PATH || 'AIRDATES.fex';
-INCLUDE &AIRDATES


-SET &WHATDATE = IF '&&DATETYPE.EVAL' EQ 'I' THEN 'INVOICE_DATE' ELSE 'PICKUP_DATE';
-SET &CARDATES = &&WEB_PATH || 'CARDATES.fex';
-INCLUDE &CARDATES    


-SET &WHATDATE = IF '&&DATETYPE.EVAL' EQ 'I' THEN 'INVOICE_DATE' ELSE 'CHKIN_DATE';
-SET &HTLDATES = &&WEB_PATH || 'HTLDATES.fex';
-INCLUDE &HTLDATES    


-SET &ADTDESC = IF &&DATETYPE EQ 'I' THEN 'TRN_DATE' ELSE 'FST_DPT_DATE';

-SET &CDTDESC = IF &&DATETYPE EQ 'I' THEN 'INVOICE_DATE' ELSE 'PICKUP_DATE';
-SET &HDTDESC = IF &&DATETYPE EQ 'I' THEN 'INVOICE_DATE' ELSE 'CHKIN_DATE';



-INCLUDE SetBDBsForDrill

-INCLUDE GetForDrillDown
-RUN



JOIN SET_KEY IN RPT_INST TO SET_KEY IN RPT_SET
TABLE FILE RPT_INST
BY ROLLUP
BY RPT_GROUP
BY RPT_TYPE
BY RPT_STREAM
BY USE_PARM
BY GLOBAL_PARM
BY RPT_NAME
WHERE RPT_TYPE EQ 'STM' 
-*AND RPT_GROUP EQ 'AIR'
-*WHERE RPT_STREAM EQ 'AIR_BNM'
-*WHERE USE_PARM EQ 'AVIW17' OR 'AVIW99'
END




MODIFY FILE ROLLUP
FIXFORM ROLLUP_CODE/A8 UPD_STAT/A1
MATCH ROLLUP_CODE
  ON MATCH UPDATE UPD_STAT
DATA 
SCHNEI-R 
END